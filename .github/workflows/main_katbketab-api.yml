# Azure Functions deployment workflow is intentionally disabled.
# The Function App and Cosmos DB were removed from Azure.
#
# If you recreate backend infrastructure later, restore this workflow
# from git history and update secrets/settings before enabling it.

name: Azure Functions deployment (disabled)

on:
  workflow_dispatch:

jobs:
  disabled_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Backend deployment disabled
        run: echo "Azure Functions deployment is intentionally disabled for this repository."
          